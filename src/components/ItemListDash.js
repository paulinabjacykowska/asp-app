import React from 'react';
import { View, StyleSheet } from 'react-native';

export default function ItemListDash() {
  return <View style={styles.dash} />;
}
const styles = StyleSheet.create({
  dash: {
    backgroundColor: '#000000',
    height: 3,
    width: 25,
  },
});
